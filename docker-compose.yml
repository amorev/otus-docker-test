version: "3"
services:
  service1:
    build: .
    expose:
      - 3000
    environment:
      - VIRTUAL_HOST=test.s3.obvu.ru
      - VIRTUAL_PORT=3000
    networks:
      - reverse-proxy

networks:
  reverse-proxy:
    external:
      name: reverse-proxy
